/*
A instrução else executa código quando a condição if é false.

if (condition) {
  // Código a ser executado quando a condição é verdadeira
} else {
  // Código a ser executado quando a condição é falsa
}
Exemplo:

void main() {
  int number = 10;
  
  if (number > 0) {
    print('The number is positive');
  } else {
    print('The number is not positive');
  }
}

Desafio

Fácil
Crie um programa que determina se um aluno passou ou reprovou em uma prova com base na sua pontuação:

Declare uma variável inteira examScore com o valor de 65
Escreva uma instrução if que verifica se a pontuação é maior ou igual a 70
Se a condição for verdadeira, imprima "Exam result: Pass"
Adicione uma instrução else que executa quando a condição é falsa
No bloco else, imprima "Exam result: Fail"
Sua saída deve corresponder exatamente ao formato esperado.

*/
void main() {
  // Declare the examScore variable here
  int examScore = _;
  
  // Write your if-else statement here

}