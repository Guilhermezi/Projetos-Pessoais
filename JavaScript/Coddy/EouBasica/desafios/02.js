/*

Desafio

Iniciante
Você recebe um código que armazena um número aleatório como entrada em uma variável chamada rnd.

Imprima "The input is: " seguido pelo valor de rnd.
Imprima:
"The number is even." se rnd for par.
"The number is odd." se rnd for ímpar.
*/
let rnd = inp; // Don't change this line
console.log("The input is: " + rnd);
if (rnd % 2 === 0) {
    console.log("The number is even.");
} else {
    console.log("The number is odd.");
}